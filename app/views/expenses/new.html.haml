.form__contents
  .col-lg-4.col-md-6.ml-auto.mr-auto
    .form__content
      .form__head
        %h2.form__text 新規タスク登録
= form_with(model: [@group, @expense], local: true, id: "form__expense") do |f|
  = render 'expenseForm', { group: @group, expense: @expense, f: f, newEdit: "NEW" }