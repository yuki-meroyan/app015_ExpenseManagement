.form__contents
  .col-lg-4.col-md-6.ml-auto.mr-auto
    .form__content
      .form__head
        %h2.form__text 新規タスク登録
.form__post__wrap
  = form_with(model: [@group, @task], local: true, id: "form__task") do |f|
    = render 'taskForm', { group: @group, task: @task, f: f}