.form__contents
  .col-lg-4.col-md-6.ml-auto.mr-auto
    .form__content
      .form__head
        = link_to new_group_path do
          %h2.form__text グループ新規作成
      .form__box
        .form__text
      - if @groups
        - @groups.each do |group|
          .form__box
            = link_to group_path(group.group.id) do
              .form__button
                = group.group.name
      .form__footer
        -# = link_to new_user_session_path do
        -#   %h2.form__text ログイン画面へ